---
import BaseLayout from '../layouts/BaseLayout.astro';
import SectionLabel from '../components/SectionLabel.astro';
import MissionScrollStory from '../components/MissionScrollStory.astro';
import TextLink from '../components/TextLink.astro';
import AudienceCard from '../components/AudienceCard.astro';
import CaseCard from '../components/CaseCard.astro';
import PartnerLogos from '../components/PartnerLogos.astro';
import ContactCta from '../components/ContactCta.astro';
import { CASES } from '../data/cases';
import { SITE_NAME, SITE_NAME_EN } from '../config';

const positioning = [
  {
    index: '01',
    title: '讓決策源自理解',
    body: '透過研究走進使用者的真實情境，理解問題如何發生，讓每一步都有脈絡可循。',
  },
  {
    index: '02',
    title: '讓全局清晰可見',
    body: '從使用者體驗到組織營運，我們梳理流程、角色及互動，找出能牽動全局的關鍵施力點。',
  },
  {
    index: '03',
    title: '讓改變真實發生',
    body: '理解多方需求與現實限制，在複雜組織中找到可行路徑，讓洞察落地、讓設計產生影響。',
  },
];
---

<BaseLayout
  title={`${SITE_NAME} ${SITE_NAME_EN}｜從複雜，致清晰`}
  description="致遠體驗設計透過研究、全局梳理與多方協作，陪政府與企業從複雜走向清晰，找出可落地的改善方向。"
  isHome
>
  {/* 重新整理一律從頁面頂端開始，不還原上次捲動位置（Hero 輪播從頭看起） */}
  <script is:inline>
    if ('scrollRestoration' in history) history.scrollRestoration = 'manual';
    window.scrollTo(0, 0);
  </script>

  {/* ---- 2. Hero：滿版海報式＋無縫循環輪播（v2.0）。開場＝品牌合併終態 →
       體驗設計退場、致遠滑回合併點 → 「遠」拉霸輪轉落定「致深」→ 上下分離回句子
       （右欄同步切 deeper）→ 深／廣／遠三幕 → 合併回品牌 → 循環 ---- */}
  <section class="bg-page">
    <div class="hero-band" data-hc>
      <div class="hero-grid" data-reveal data-reveal-once>
        {/* 左欄：主標（單一 H1；朗讀內容固定，動畫層 aria-hidden） */}
        <h1 class="heading-hero relative" data-hc-stage>
          <span class="sr-only">從複雜，致清晰，陪你走得更深、更廣、更遠。致遠體驗設計。</span>

          {/* 靜態備援：無 JS 或 reduced-motion 時顯示 */}
          <span class="hc-static" aria-hidden="true">
            <span class="block">從複雜，<span class="text-brand">致</span>清晰</span>
            <span class="mt-3 block">陪你走得更深、更廣、更<span class="text-brand">遠</span></span>
          </span>

          {/* 輪播動畫層：初始態＝品牌開場（O1），兩行句子預設隱藏（is-faded），
              避免 JS 就位前閃出原版標題；品牌行由腳本量測定位後顯示 */}
          <span class="hc-anim" aria-hidden="true">
            <span class="hc-line is-faded block" data-hc-line1
              >從複雜，<span class="text-brand" data-hc-zhi>致</span>清晰</span
            >
            <span class="hc-line is-faded mt-3 block" data-hc-line2
              >陪你走得更<span class="hc-slot text-brand" data-hc-left
                ><span class="hc-slide-item">深</span><span class="hc-slide-item">廣</span
                ><span class="hc-slide-item">遠</span></span
              ></span
            >
            {/* 品牌行：致遠合併後「體驗設計」自上方滑入；「遠」為字槽，
                開頭段落在此拉霸輪轉（遠→深→廣→…→深）後上下分離回句子 */}
            <span class="hc-brand" data-hc-brand
              ><span class="text-brand hc-brand-char" data-hc-brand-zhi>致</span><span
                class="hc-slot text-brand hc-brand-char"
                data-hc-brand-slot
                ><span class="hc-slide-item is-current" data-hc-brand-yuan>遠</span><span
                  class="hc-slide-item">深</span
                ><span class="hc-slide-item">廣</span></span
              ><span class="hc-brand-rest"
                ><span class="hc-slide-item" data-hc-brand-rest>體驗設計</span></span
              ></span
            >
          </span>
        </h1>

        {/* 右欄：Reach 句＋對應中文句（無卡片外框） */}
        <div class="hc-right">
          <p class="sr-only">
            Reach deeper understanding，讓決策源自理解。Reach the bigger
            picture，讓全局清晰可見。Reach change that matters，讓改變真實發生。Reach
            Experience Design。
          </p>

          {/* 靜態備援 */}
          <div class="hc-static" aria-hidden="true">
            <p class="hc-en-static"><span class="text-brand">Reach</span> deeper understanding</p>
            <p class="hc-en-static"><span class="text-brand">Reach</span> the bigger picture</p>
            <p class="hc-en-static"><span class="text-brand">Reach</span> change that matters</p>
            <p class="hc-en-static"><span class="text-brand">Reach</span> better experiences</p>
          </div>

          {/* 輪播動畫層：Reach 固定不動；詞組與中文句垂直滑動輪替（舊字下滑出、
              新字上方進）。初始態＝品牌開場（O1）：Experience／Design 預設 is-current */}
          <div class="hc-anim hc-right-anim" aria-hidden="true">
            <p class="hc-en"><span class="text-brand">Reach</span></p>
            <div class="hc-en hc-slide" data-hc-phrase>
              <span class="hc-slide-item">deeper understanding</span>
              <span class="hc-slide-item">the bigger picture</span>
              <span class="hc-slide-item">change that matters</span>
              <span class="hc-slide-item is-current">Experience</span>
            </div>
            <div class="hc-en hc-slide hc-line3" data-hc-zh>
              <span class="hc-slide-item hc-zh-item">讓決策源自理解</span>
              <span class="hc-slide-item hc-zh-item">讓全局清晰可見</span>
              <span class="hc-slide-item hc-zh-item">讓改變真實發生</span>
              <span class="hc-slide-item is-current">Design</span>
            </div>
          </div>
        </div>
      </div>
    </div>
  </section>

  {/* ---- 3. Why We Do／我們的使命（捲動敘事版，見 MissionScrollStory） ---- */}
  <MissionScrollStory items={positioning} />

  {/* ---- 3.5 合作客戶 Logo 輪播（我們的使命下方；企業與政府交錯混排、左右淡出）。
       上下 padding 比照「我們的經驗」（sec-full 的內距、不撐滿版高） ---- */}
  <section class="bg-page">
    <h2 class="sr-only">合作客戶</h2>
    <div class="py-16 md:pt-20 md:pb-12" data-reveal>
      <PartnerLogos />
    </div>
  </section>

  {/* ---- 4. What We Do／我們的服務 ---- */}
  <section class="border-t border-line bg-page">
    <div class="container-site sec-full">
      <SectionLabel zh="我們的服務" en="WHAT WE DO" />
      <div class="mt-10 grid items-center gap-12 lg:grid-cols-[1fr_1.05fr] lg:gap-16">
        <div data-reveal>
          <h3 class="heading-sub max-w-xl">從未知到已知，讓改變有清楚的方向</h3>
          <p class="mt-5 max-w-xl leading-loose text-ink-secondary">
            面對複雜問題，我們從真實情境出發，結合研究、全局梳理與多方協作，釐清真正影響成效的關鍵，形成可執行的改善方向。
          </p>
          <p class="mt-8">
            <TextLink href="/services/" label="服務項目" />
          </p>
        </div>

        {/* 未知到已知：六個重疊圓由左至右、透明度遞增至品牌原色（無文字符號） */}
        <div data-reveal>
          <svg
            viewBox="0 0 560 280"
            class="wd-visual h-auto w-full"
            role="img"
            aria-label="示意圖：六個圓形由左至右重疊排列，顏色由淺至品牌藍原色，象徵從未知逐步走向已知"
          >
            {/* 實色（原透明度混色的烘焙值）；繪製順序左→右，右圓疊在左圓之上 */}
            <circle class="wd-circle" cx="80" cy="140" r="80" fill="#DFE8FA"></circle>
            <circle class="wd-circle" cx="156" cy="140" r="80" fill="#CCDCFB"></circle>
            <circle class="wd-circle" cx="232" cy="140" r="80" fill="#B0C9FC"></circle>
            <circle class="wd-circle" cx="308" cy="140" r="80" fill="#86ADFC"></circle>
            <circle class="wd-circle" cx="384" cy="140" r="80" fill="#4D88FE"></circle>
            <circle class="wd-circle" cx="460" cy="140" r="80" fill="var(--reach-blue-500)"></circle>
          </svg>
        </div>
      </div>

    </div>
  </section>

  {/* ---- 5. 精選案例／我們的經驗（緊接我們的服務） ---- */}
  <section class="border-t border-line bg-page">
    <div class="container-site sec-full">
      <div class="flex flex-wrap items-baseline justify-between gap-4" data-reveal>
        <SectionLabel zh="我們的經驗" en="OUR EXPERIENCE" />
        <TextLink href="/cases/" label="所有案例" />
      </div>
      <div class="mt-10 grid gap-x-8 gap-y-14 sm:grid-cols-2" data-reveal>
        {CASES.map((item) => <CaseCard {...item} />)}
      </div>
    </div>
  </section>

  {/* ---- 6. 我們與誰合作（獨立區塊，與其他區塊同級） ---- */}
  <section class="border-t border-line bg-page">
    <div class="container-site sec-full">
      <div data-reveal>
        <SectionLabel zh="我們與誰合作" en="WHO WE WORK WITH" />
        <div class="mt-10 grid items-center gap-12 lg:grid-cols-[1fr_1.05fr] lg:gap-16">
          <div>
            <h3 class="heading-sub max-w-xl">解法因情境而異，核心始終如一</h3>
            <p class="mt-5 max-w-xl leading-loose text-ink-secondary">
              面對不同的目標、限制與協作方式，我們找出合適的切入點，讓解法符合情境，也能真正落地。
            </p>
          </div>

          {/* 三圓圖示（無線條，三階品牌藍色塊，與「我們的服務」六圓同色階）。
              二段式進場動畫：初始三圓相同（上淺下中階）；
              第一段同時旋轉（左 270°、中 180°、右 180°＋中階色塊邊轉邊長大）；
              第二段同時（左：深色弓形以圓頂點為軸旋開露出；中：內圓再轉 180°、
              跨過水平線的部分即為深色；右：再轉 180°＋深色底塊自圓周外滑入）。
              一律幾何呈現（旋轉／滑入／裁切），無漸變；皆帶回彈（overshoot 後彈回）。
              終態與靜態版完全一致；無 JS／reduced-motion 直接顯示終態。 */}
          <svg
            viewBox="0 0 660 220"
            class="ww-visual h-auto w-full"
            role="img"
            aria-label="示意圖：三個圓形各以不同方式分層填色，色塊代表不同情境下的切入點"
          >
            <defs>
              <clipPath id="ww-clip-a"><circle cx="110" cy="110" r="90"></circle></clipPath>
              {/* 左圓右半平面：深色層與遮板都限制在右半 */}
              <clipPath id="ww-clip-a-r"><rect x="110" y="18" width="94" height="184"></rect></clipPath>
              <clipPath id="ww-clip-b"><circle cx="330" cy="110" r="90"></circle></clipPath>
              {/* 中圓水平線以下：深色半圓只在跨過水平線的部分顯示 */}
              <clipPath id="ww-clip-b-low"><rect x="238" y="110" width="184" height="94"></rect></clipPath>
              {/* 中心左側水平線上方 0–20° 小楔形：僅供回彈超轉（>180°）時
                  左側跨線小色塊顯示最深色（搭配時間閘門避免第二段初始閃現） */}
              <clipPath id="ww-clip-b-wedge"><path d="M 330 110 L 238 110 L 238 76.5 Z"></path></clipPath>
              <clipPath id="ww-clip-c"><circle cx="550" cy="110" r="90"></circle></clipPath>
              {/* 右圓 80 以上頂帶：旋入的深色只在落定區域內顯示 */}
              <clipPath id="ww-clip-c-top"><rect x="458" y="16" width="184" height="64"></rect></clipPath>
            </defs>

            {/* 圓一：整體轉 270° → 第二段：深色右半圓形以頂點 (110,20) 為軸旋入。
                初始轉 90°（整片在圓外，不可見），旋回 25° 時其直邊恰為 130° 弓形的弦
                （頂點圓周角＝圓心角一半），露出的深色恆為貼著圓弧的弓形；
                回彈超過 25° 時弓形真的變大再縮回。 */}
            <g clip-path="url(#ww-clip-a)">
              <g class="ww-a-spin">
                <circle cx="110" cy="110" r="90" class="ww-l"></circle>
                <rect x="18" y="110" width="184" height="94" class="ww-m"></rect>
              </g>
              <g clip-path="url(#ww-clip-a-r)">
                <path d="M 110 20 A 90 90 0 0 1 110 200 Z" class="ww-d ww-a-dark"></path>
              </g>
            </g>

            {/* 圓二：整體轉 180° → 內圓再轉 180°。深色＝獨立的同步旋轉半圓，
                以靜態「水平線以下」裁切：只有跨過水平線的部分顯示為深色（非漸變）。
                內圓中階半圓維持中階色，與深色層同步旋轉、始終重合。 */}
            <g clip-path="url(#ww-clip-b)">
              <g class="ww-b-spin">
                <circle cx="330" cy="110" r="90" class="ww-l"></circle>
                <rect x="238" y="110" width="184" height="94" class="ww-m"></rect>
                <g class="ww-b-inner">
                  <path d="M 278 111 A 52 52 0 0 1 382 111 Z" class="ww-l"></path>
                  <path d="M 278 110 A 52 52 0 0 0 382 110 Z" class="ww-m"></path>
                </g>
              </g>
              <g clip-path="url(#ww-clip-b-low)">
                <g class="ww-b-darkspin">
                  <path d="M 278 110 A 52 52 0 0 1 382 110 Z" class="ww-d"></path>
                </g>
              </g>
              {/* 楔形層：同步旋轉的深色複本，時間閘門在第二段開始 285ms 後開啟
                 （彼時旋轉已過 20°、楔形必為空，開啟無感）；僅回彈超轉時顯示 */}
              <g clip-path="url(#ww-clip-b-wedge)" class="ww-b-wedgegate">
                <g class="ww-b-darkspin">
                  <path d="M 278 110 A 52 52 0 0 1 382 110 Z" class="ww-d"></path>
                </g>
              </g>
            </g>

            {/* 圓三（上深中中下淺）：第一段轉 180°，中階半平面同時把邊界外推 30——
                翻到上方蓋住 20–140 後定住；第二段最深色獨自旋入落定於最上層：
                深色＝通過圓心的下半圓（比照圓二），隨 capspin 順時針轉半圈變上半圓，
                靜態頂帶裁切只顯示跨入 20–80 落定區的部分，超轉時頂帶保持全滿。 */}
            <g clip-path="url(#ww-clip-c)">
              <g class="ww-c-spin1">
                <circle cx="550" cy="110" r="90" class="ww-l"></circle>
                <rect x="458" y="110" width="184" height="126" class="ww-m ww-c-med"></rect>
              </g>
              <g clip-path="url(#ww-clip-c-top)">
                <g class="ww-c-capspin">
                  <path d="M 460 110 A 90 90 0 0 0 640 110 Z" class="ww-d"></path>
                </g>
              </g>
            </g>
          </svg>
        </div>
      </div>
      <div class="mt-10 grid gap-5 md:grid-cols-2" data-reveal>
        <AudienceCard
          pill="政府與公共服務"
          title="深耕數位公共服務，始終以民眾為本"
          em="以民眾為本"
          body="我們熟悉政府標案、跨單位協作與既有系統限制，在民眾需要、政策目標與執行現實之間，找到真正可行的改善方向。"
          href="/public-sector/"
          ariaLabel="查看更多：政府與公共服務"
        />
        <AudienceCard
          pill="企業與服務團隊"
          title="找對問題，讓體驗改善轉為商業價值"
          em="轉為商業價值"
          body="從使用者、服務流程與組織全局中找出關鍵施力點，讓重大決策建立在理解之上。"
          href="/business/"
          ariaLabel="查看更多：企業與服務團隊"
        />
      </div>
    </div>
  </section>

  {/* ---- AI 主張（移至我們的經驗上方）---- */}
  <section class="border-t border-line bg-page">
    <div class="container-site py-30 text-center md:py-42" data-reveal>
      <h2 class="heading-section mx-auto max-w-4xl">
        AI 讓產出<span class="ai-em">更快速</span>，我們讓成果<span class="ai-em">更精準</span>
      </h2>
      <p class="mx-auto mt-6 max-w-2xl leading-loose text-ink-secondary">
        AI 能快速產出，但產出的價值仍取決於對問題的理解深度。<br
          class="hidden md:inline"
        />我們結合 AI 的效率、使用者研究與全局判斷，讓成果真正回應需求。
      </p>
      <div class="mt-9 flex justify-center">
        <a href="/services/" class="ai-cta">
          了解更多
          <span class="cta-arrow" aria-hidden="true">→</span>
        </a>
      </div>
    </div>
  </section>

  {/* ---- 8. 最終聯絡 CTA ---- */}
  <ContactCta />

  <script>
    import '../scripts/hero-carousel';
  </script>
</BaseLayout>

<style>
  /* AI 主張 CTA：藍框白底藍字 */
  .ai-cta {
    display: inline-flex;
    align-items: center;
    gap: 0.5rem;
    min-height: 2.75rem;
    padding: 0.625rem 1.75rem;
    border-radius: 9999px;
    border: 1px solid var(--color-brand);
    background-color: var(--color-surface);
    color: var(--color-link);
    font-weight: 600;
    text-decoration: none;
    transition: background-color 200ms ease, transform 200ms ease;
  }

  .ai-cta:hover {
    background-color: var(--reach-blue-50);
  }

  .ai-cta:active {
    transform: translateY(1px);
  }

  .ai-cta:focus-visible {
    outline: 2px solid var(--reach-blue-300);
    outline-offset: 2px;
  }

  .ai-cta .cta-arrow {
    transition: transform 200ms ease;
  }

  .ai-cta:hover .cta-arrow,
  .ai-cta:focus-visible .cta-arrow {
    transform: translateX(3px);
  }

  @media (prefers-reduced-motion: reduce) {
    .ai-cta,
    .ai-cta .cta-arrow {
      transition: none;
    }
    .ai-cta:hover .cta-arrow {
      transform: none;
    }
  }

  /* AI 主張強調詞：藍色螢光筆畫記，進場時由左至右掃過（兩詞同時） */
  .ai-em {
    color: var(--color-text);
    background-image: linear-gradient(var(--reach-blue-200), var(--reach-blue-200));
    background-repeat: no-repeat;
    background-size: 100% 100%;
    background-position: 0 50%;
    padding-inline: 0.06em;
    margin-inline: -0.03em;
  }

  @media (prefers-reduced-motion: no-preference) {
    :global(html.js) .ai-em {
      background-size: 0% 100%;
      transition: background-size 550ms cubic-bezier(0.65, 0, 0.35, 1);
      transition-delay: 250ms; /* 兩詞同時開始 */
    }

    :global(html.js) [data-reveal].is-visible .ai-em {
      background-size: 100% 100%;
    }

    /* 離場即瞬間重置，捲回時螢光筆重新掃入 */
    :global(html.js) [data-reveal]:not(.is-visible) .ai-em {
      transition: none;
    }
  }

  /* ---- 滿版區塊：比照「我們的使命」（msp-frame padding-block 5rem 3rem、
     高度＝視窗高 − header），套用於服務／誰合作／經驗／信任四區；
     AI 與聯絡 CTA 除外。內容高於視窗時自然生長，padding 為最小間距 ---- */
  .sec-full {
    padding-block: 4rem;
  }

  @media (min-width: 768px) {
    .sec-full {
      display: flex;
      flex-direction: column;
      justify-content: center;
      min-height: calc(100svh - 4.5rem);
      padding-block: 5rem 3rem;
    }
  }

  /* ---- 滿版 Hero：內距約 80px（5.5vw 封頂），桌機高度撐到視窗高扣掉 header ---- */
  .hero-band {
    padding-inline: clamp(1.25rem, 5.5vw, 5rem);
    padding-block: 5rem 4rem;
    overflow-x: clip; /* 垂直合併期間品牌行的隱形佔位盒可能超出，夾住避免水平捲動 */
  }

  @media (min-width: 768px) {
    .hero-band {
      display: flex;
      flex-direction: column;
      justify-content: center;
      /* header 桌機高 72px；svh 避免行動瀏覽器網址列造成跳動 */
      min-height: calc(100svh - 4.5rem);
      padding-block: 3rem;
    }
  }

  /* ---- Hero 兩欄 ----
     --hero-fs 與 .heading-hero 的 clamp 公式一致，右欄字級按比例推導以維持左右等高 */
  .hero-grid {
    --hero-fs: clamp(2.5rem, 5.25vw - 3px, 4.125rem);
    /* 左欄前導內距：讓主標離左緣遠一點、靠近中間（手機為 0，md+ 於下方 media 設值） */
    --hero-lead: 0rem;
    /* 字槽滑動：時長與緩動（開頭漸快、結尾漸慢 easeInOutQuint）在此統一調整；
       SLIDE_MS in hero-carousel.ts 需與 --hc-dur 同步 */
    --hc-dur: 850ms;
    --hc-fly-dur: 850ms; /* 幕3→4 致遠合併飛行：與 --hc-dur 同步（與右欄切換同始同終） */
    --hc-fly-ease: cubic-bezier(0.65, 0, 0.35, 1); /* 飛行用 easeInOutCubic：中段較緩 */
    --hc-ease: cubic-bezier(0.83, 0, 0.17, 1);
    display: grid;
    gap: 2.75rem;
    align-items: center;
    transition: opacity 440ms ease;
  }

  @media (min-width: 768px) {
    .hero-grid {
      /* 前導內距隨寬度遞增（768 起約 17px → 1440 約 71px），推左標靠中間 */
      --hero-lead: clamp(0rem, 8vw - 2.75rem, 5rem);
      grid-template-columns: 1fr 1fr;
      gap: 3rem;
    }

    .hero-grid > h1 {
      padding-inline-start: var(--hero-lead);
    }

    /* 右欄撐滿列高（列高由左欄 H1 決定），三行以 space-between 對齊上下緣 */
    .hc-right {
      align-self: stretch;
      display: flex;
      flex-direction: column;
      justify-content: center;
    }
  }

  /* 分隔線：貼齊右欄區塊左緣，高度＝文字區塊高度 */
  .hc-right {
    border-inline-start: 2px solid var(--color-border);
    padding-inline-start: clamp(1.25rem, 3vw, 3rem);
  }

  [data-hc].is-resetting .hero-grid {
    opacity: 0;
  }

  /* ---- 輪播／靜態備援切換：預設靜態，JS＋允許動態時啟用輪播 ---- */
  .hc-static {
    display: block;
  }

  .hc-anim {
    display: none;
  }

  @media (prefers-reduced-motion: no-preference) {
    :global(html.js) .hc-anim {
      display: block;
    }

    :global(html.js) .hc-static {
      display: none;
    }
  }

  /* ---- 左欄動畫層 ---- */
  .hc-line {
    transition: opacity 850ms ease;
  }

  .hc-line.is-faded {
    opacity: 0;
  }

  .is-ghost {
    visibility: hidden;
  }

  /* 左欄字槽：固定 1em 寬、與行高同高的滑動遮罩。
     overflow:hidden 的 inline-block 基線會落在盒底，用 vertical-align:bottom 對齊行框 */
  .hc-slot {
    display: inline-block;
    position: relative;
    overflow: hidden;
    width: 1em;
    height: 1.12em;
    vertical-align: bottom;
  }

  /* 第四幕品牌行：疊在第一行位置，等致遠合併後顯示 */
  .hc-brand {
    /* 最終幕品牌名：垂直置中；水平位置由 JS 對齊「致」原始 x（垂直合併），
       合併完成後以 transform 整行滑回置中 */
    position: absolute;
    inset-block: 0;
    inset-inline-start: 0;
    display: flex;
    align-items: center;
    white-space: nowrap;
    pointer-events: none;
    transition: transform 700ms var(--hc-ease, cubic-bezier(0.83, 0, 0.17, 1));
  }

  .hc-brand .hc-brand-char {
    opacity: 0;
    transition: opacity 180ms ease;
  }

  .hc-brand.is-shown .hc-brand-char {
    opacity: 1;
  }

  /* 「體驗設計」滑入遮罩（寬 4 個全形字） */
  .hc-brand-rest {
    display: inline-block;
    position: relative;
    overflow: hidden;
    width: 4em;
    height: 1.12em;
    vertical-align: bottom;
  }

  /* 拉霸連續捲軸（由 JS 建立）：整串字直向堆疊，單一 transition 從頭捲到尾。
     每格高度與 .hc-slot 的 1.12em 一致，字形在格內位置與字槽實體字重合 */
  :global(.hc-spin-strip) {
    position: absolute;
    top: 0;
    inset-inline-start: 0;
    width: 100%;
    will-change: transform;
  }

  :global(.hc-spin-strip .hc-spin-cell) {
    display: block;
    height: 1.12em;
  }

  /* FLIP 飛行字（致、遠）：由 JS 建立，繼承 h1 字體樣式 */
  :global(.hc-clone) {
    position: absolute;
    margin: 0;
    color: var(--color-brand);
    transition: transform var(--hc-fly-dur, 850ms) var(--hc-fly-ease, cubic-bezier(0.65, 0, 0.35, 1));
    will-change: transform;
    pointer-events: none;
  }

  /* ---- 右欄（v1.1.2：三行結構 Reach／詞組／中文句，與左欄 H1 等高）---- */
  .hc-right-anim {
    gap: 0.25rem;
  }

  .hc-right-anim > * {
    flex-shrink: 0;
  }

  @media (prefers-reduced-motion: no-preference) {
    :global(html.js) .hc-right-anim {
      display: flex;
      flex-direction: column;
    }
  }

  @media (min-width: 768px) {
    .hc-right-anim {
      flex: 1;
      justify-content: space-between;
    }
  }

  .hc-en {
    font-size: 1.9375rem; /* 手機 31px：含分隔線內距後最長詞組仍有安全邊距 */
    line-height: 1.15;
    font-weight: 600;
    color: var(--color-text);
    margin: 0;
  }

  /* 第三行：與第二行的距離（v1.4 依業主目測回饋），隨主標字級縮放 */
  .hc-line3 {
    margin-block-start: calc(var(--hero-fs, 2.5rem) * 0.05);
  }

  /* 幕 1–3 的中文句：維持原字級與字重，於等高字槽內垂直置中 */
  .hc-zh-item {
    font-size: 1.375rem;
    line-height: calc(1.9375rem * 1.15);
    font-weight: 400;
    color: var(--color-text-secondary);
  }

  /* 由主標字級推導；v1.9 右欄字級盡量貼近左欄（md 0.68、lg+ 0.75）。
     右塊自然高度略高於左欄兩行，由 grid align-items:center 互相垂直置中。 */
  @media (min-width: 768px) {
    .hc-en {
      font-size: calc(var(--hero-fs) * 0.68);
    }

    .hc-zh-item {
      font-size: calc(var(--hero-fs) * 0.44);
      line-height: calc(var(--hero-fs) * 0.68 * 1.15);
    }
  }

  @media (min-width: 1024px) {
    .hc-en {
      font-size: calc(var(--hero-fs) * 0.75);
    }

    .hc-zh-item {
      line-height: calc(var(--hero-fs) * 0.75 * 1.15);
    }
  }


  /* 垂直滑動字槽：舊字下滑出、新字自上方進（機制同 Animate UI「Rotating Text」，原生實作） */
  .hc-slide {
    position: relative;
    overflow: hidden;
    height: 1.15em;
  }

  .hc-slide-item {
    position: absolute;
    top: 0;
    inset-inline-start: 0;
    white-space: nowrap;
    opacity: 0;
    transform: translateY(-130%);
    transition:
      transform var(--hc-dur, 680ms) var(--hc-ease, cubic-bezier(0.83, 0, 0.17, 1)),
      opacity calc(var(--hc-dur, 680ms) * 0.7) ease;
  }

  .hc-slide-item.is-current {
    opacity: 1;
    transform: none;
  }

  .hc-slide-item.is-leaving {
    opacity: 0;
    transform: translateY(130%);
  }

  .hc-slide-item.no-anim {
    transition: none;
  }

  /* 靜態備援（無 JS／reduced-motion） */
  .hc-en-static {
    font-size: clamp(1.875rem, 1.5rem + 1.5vw, 2.4375rem);
    line-height: 1.3;
    font-weight: 600;
    color: var(--color-text);
  }

  .hc-en-static + .hc-en-static {
    margin-top: 0.75rem;
  }

  /* ---- 我們與誰合作：三圓切分圖示 ---- */
  .ww-visual {
    max-width: 560px;
  }

  /* 三階藍與「我們的服務」六圓色階一致 */
  .ww-l {
    fill: #dfe8fa;
  }

  .ww-m {
    fill: #b0c9fc;
  }

  .ww-d {
    fill: #4d88fe;
  }

  /* ---- 三圓二段式進場動畫 ----
     基態（無 JS／reduced-motion）＝終態；以下 transform 即終態擺位。
     時序：--wwa-t1 第一段開始、--wwa-t2 第二段開始，三圓同起同收。 */
  .ww-visual {
    --wwa-d1: 950ms;
    --wwa-d2: 950ms;
    --wwa-t1: 300ms;
    --wwa-t2: 1400ms; /* t1 + d1 + 150ms 間隔 */
    --wwa-spring: cubic-bezier(0.34, 1.4, 0.64, 1); /* overshoot 回彈 */
  }

  .ww-a-spin,
  .ww-a-dark,
  .ww-b-spin,
  .ww-b-inner,
  .ww-b-darkspin,
  .ww-c-spin1,
  .ww-c-capspin {
    transform-box: view-box;
  }

  .ww-a-spin {
    transform-origin: 110px 110px;
    transform: rotate(270deg);
  }

  /* 深色右半圓的直邊停在 130° 弓形的弦上（頂點圓周角為圓心角一半＝65°，
     即自垂直徑偏 25°；CSS 正角向左轉出，故取 -25°） */
  .ww-a-dark {
    transform-origin: 110px 20px;
    transform: rotate(-25deg);
  }

  .ww-b-spin,
  .ww-b-inner,
  .ww-b-darkspin {
    transform-origin: 330px 110px;
    transform: rotate(180deg);
  }

  .ww-b-wedgegate {
    opacity: 1;
  }

  .ww-c-spin1,
  .ww-c-capspin {
    transform-origin: 550px 110px;
    transform: rotate(180deg);
  }

  /* 中階半平面邊界外推 30：110 → 80（局部座標；終態即長大後位置） */
  .ww-c-med {
    transform: translateY(-30px);
  }

  @media (prefers-reduced-motion: no-preference) {
    /* 初始態（進場前）：三圓一致＝上淺下中階；深色層與增量帶
       以「隱藏／圓周外」待命，全程無漸變 */
    :global(html.js) [data-reveal]:not(.is-visible) .ww-a-spin,
    :global(html.js) [data-reveal]:not(.is-visible) .ww-b-spin,
    :global(html.js) [data-reveal]:not(.is-visible) .ww-b-inner,
    :global(html.js) [data-reveal]:not(.is-visible) .ww-b-darkspin,
    :global(html.js) [data-reveal]:not(.is-visible) .ww-c-spin1,
    :global(html.js) [data-reveal]:not(.is-visible) .ww-c-capspin {
      transform: rotate(0deg);
    }

    /* 深色右半圓初始轉 -90°：整片在切線外側（圓外），不可見 */
    :global(html.js) [data-reveal]:not(.is-visible) .ww-a-dark {
      transform: rotate(-90deg);
    }

    :global(html.js) [data-reveal]:not(.is-visible) .ww-b-wedgegate {
      opacity: 0;
    }

    :global(html.js) [data-reveal]:not(.is-visible) .ww-c-med {
      transform: translateY(0);
    }

    /* 離場即瞬間重置（不跑回程的分段 transition）：否則快速捲回時
       深色層還停在終態，重播會一開始就整片出現 */
    :global(html.js) [data-reveal]:not(.is-visible) .ww-a-spin,
    :global(html.js) [data-reveal]:not(.is-visible) .ww-a-dark,
    :global(html.js) [data-reveal]:not(.is-visible) .ww-b-spin,
    :global(html.js) [data-reveal]:not(.is-visible) .ww-b-inner,
    :global(html.js) [data-reveal]:not(.is-visible) .ww-b-darkspin,
    :global(html.js) [data-reveal]:not(.is-visible) .ww-b-wedgegate,
    :global(html.js) [data-reveal]:not(.is-visible) .ww-c-spin1,
    :global(html.js) [data-reveal]:not(.is-visible) .ww-c-capspin,
    :global(html.js) [data-reveal]:not(.is-visible) .ww-c-med {
      transition: none;
    }

    /* 第一段：三圓同時旋轉；右圓中階色塊邊轉邊長大（邊界外推） */
    :global(html.js) .ww-a-spin,
    :global(html.js) .ww-b-spin,
    :global(html.js) .ww-c-spin1,
    :global(html.js) .ww-c-med {
      transition: transform var(--wwa-d1) var(--wwa-spring) var(--wwa-t1);
    }

    /* 第二段：左深色半圓旋入、中內圓與深色層同步翻面、
       右最深色旋入頂帶 */
    :global(html.js) .ww-a-dark,
    :global(html.js) .ww-b-inner,
    :global(html.js) .ww-b-darkspin,
    :global(html.js) .ww-c-capspin {
      transition: transform var(--wwa-d2) var(--wwa-spring) var(--wwa-t2);
    }

    /* 楔形時間閘門：第二段開始 285ms 後瞬間開啟（旋轉已過 20°、楔形為空，
       開啟無感）；此後深色僅在回彈超轉 >180° 時進入楔形顯示 */
    :global(html.js) .ww-b-wedgegate {
      transition: opacity 20ms linear calc(var(--wwa-t2) + 285ms);
    }
  }

  /* ---- 未知到已知視覺：六圓由左至右依序浮現（捲出重置、捲回重播） ---- */
  .wd-visual {
    max-width: 560px;
  }

  @media (prefers-reduced-motion: no-preference) {
    :global(html.js) .wd-circle {
      opacity: 0;
      transform: translateX(-18px);
      transform-box: fill-box;
      transition: opacity 500ms ease, transform 500ms cubic-bezier(0.22, 1, 0.36, 1);
    }

    :global(html.js) [data-reveal].is-visible .wd-circle {
      opacity: 1;
      transform: none;
    }

    :global(html.js) [data-reveal].is-visible .wd-circle:nth-child(1) { transition-delay: 0ms; }
    :global(html.js) [data-reveal].is-visible .wd-circle:nth-child(2) { transition-delay: 110ms; }
    :global(html.js) [data-reveal].is-visible .wd-circle:nth-child(3) { transition-delay: 220ms; }
    :global(html.js) [data-reveal].is-visible .wd-circle:nth-child(4) { transition-delay: 330ms; }
    :global(html.js) [data-reveal].is-visible .wd-circle:nth-child(5) { transition-delay: 440ms; }
    :global(html.js) [data-reveal].is-visible .wd-circle:nth-child(6) { transition-delay: 550ms; }

    /* 離場即瞬間重置，確保捲回時從頭依序浮現 */
    :global(html.js) [data-reveal]:not(.is-visible) .wd-circle {
      transition: none;
    }
  }

  .ab-label {
    font-size: 15px;
    font-weight: 700;
    fill: var(--neutral-500);
  }

  @media (prefers-reduced-motion: no-preference) {
    :global(html.js) .ab-main {
      stroke-dasharray: 200;
      stroke-dashoffset: 200;
      transition: stroke-dashoffset 700ms ease 250ms;
    }

    :global(html.js) [data-reveal].is-visible .ab-main {
      stroke-dashoffset: 0;
    }

    :global(html.js) .ab-dots circle {
      opacity: 0;
      transition: opacity 400ms ease;
    }

    :global(html.js) [data-reveal].is-visible .ab-dots circle {
      opacity: 1;
    }

    :global(html.js) [data-reveal].is-visible .ab-dots circle:nth-child(1) { transition-delay: 350ms; }
    :global(html.js) [data-reveal].is-visible .ab-dots circle:nth-child(2) { transition-delay: 500ms; }
    :global(html.js) [data-reveal].is-visible .ab-dots circle:nth-child(3) { transition-delay: 650ms; }
    :global(html.js) [data-reveal].is-visible .ab-dots circle:nth-child(4) { transition-delay: 800ms; }
  }

</style>
